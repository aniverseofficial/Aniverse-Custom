# Category Click Fix

Fixed the All Categories directory so clicking a merchandise category uses the same category matching rules as the navigation.

In particular, legacy product data using `Figures` and newer product data using `Figurines` are treated as the same merchandise category. Similar aliases are handled for Qposket and Funko Pop variants.

No anime/world categories were added to All Categories.
